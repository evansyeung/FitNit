<% include ../partials/header %>
    <div class="container">
        <table class="table table-hover table-striped">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Height</th>
                    <th>Weight</th>
                    <th>Chest</th>
                    <th>Waist</th>
                    <th>Hip</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <th><%= moment(measurement.createdAt).format("MMM DD, YYYY") %></th>
                    <th><%= measurement.height %></th>
                    <th><%= measurement.weight %></th>
                    <th><%= measurement.chest %></th>
                    <th><%= measurement.waist %></th>
                    <th><%= measurement.hip %></th>
                </tr>
            </tbody>
        </table>
        <a class="btn btn-warning btn-md pull-right" href="/measurements/<%= measurement._id %>/edit">Edit <i class="fa fa-cog"></i></a></td>
        <form id="delete-form" action="/measurements/<%= measurement._id %>?_method=DELETE" method="POST">
            <button class="btn btn-danger">Delete <i class="fa fa-trash"></i></button>
        </form>
    </div>
<% include ../partials/footer %>